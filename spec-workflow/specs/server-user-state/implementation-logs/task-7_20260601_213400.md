# Task 7 — Hono app skeleton + CORS + structured-log wrapper + `/api/health` in `server/app.js`

**Spec:** server-user-state
**Task ID:** 7
**Completed:** 2026-06-01 21:34:00

## Changes

| File | Action | Notes |
|---|---|---|
| `server/app.js` | Created | ESM. Imports `Hono` from `'hono'`, `getDb` from `'./db.js'`, `validateCode` + `payloadByteLength` from `'./validators.js'`, `checkAndIncrement` from `'./rate-limit.js'` (last four are pre-wired for task 8, intentionally unused here). Module-top helper `resolveIp(c)` reads `CF-Connecting-IP || X-Real-IP || X-Forwarded-For.split(',')[0].trim() || 'unknown'` via `c.req.header.bind(c.req)`. First `app.use('*')` is the CORS middleware: sets `Access-Control-Allow-Origin` to `new URL(c.req.url).origin`, returns 204 on `OPTIONS`. Second `app.use('*')` is the structured-log wrapper: snapshots `t0 = Date.now()`, resolves `ip`, `c.set('ip', ip)`, then `try { await next(); } finally { console.log(JSON.stringify({ ts, ip, code, method, bytes, status, latency_ms })) }` — the `finally` guarantees 4xx early-return paths still log. `GET /api/health` returns `c.json({ ok: true })` with no DB call. Default export is `app`. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/server/app.js` — Hono skeleton, two `app.use('*')` middlewares (CORS, structured log), one health route. All five named imports present; the four unused ones are pre-wired for task 8 to consume without further imports.

## Verification

- `node --check server/app.js` — clean (no syntax errors).

## Requirements Traced

- **R10#3 (structured log; finally pattern catches 4xx early returns)** — the log wrapper uses `try/finally` around `await next()`, so any handler that throws or returns early (validators 400, payload 413, rate-limit 429) still emits a single structured JSON line on stdout with `code`, `bytes`, `status`, and `latency_ms`. Slots `c.set('code', ...)` / `c.set('bytes', ...)` are read with `|| '-'` / `|| 0` fallbacks so task 8 can populate them per route without touching this middleware.
