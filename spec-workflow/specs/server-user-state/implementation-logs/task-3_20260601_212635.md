# Task 3 — Create initial Turso migrations

**Spec:** server-user-state
**Task ID:** 3
**Completed:** 2026-06-01 21:26:35

## Changes

| File | Action | Notes |
|---|---|---|
| `migrations/001_init.sql` | Created | Two statements separated by `;\n`: `CREATE TABLE IF NOT EXISTS user_state (...)` + `CREATE INDEX IF NOT EXISTS idx_user_state_updated_at ON user_state (updated_at)`. No transactions, no destructive ops. |
| `migrations/002_rate_limit.sql` | Created | Single statement: `CREATE TABLE IF NOT EXISTS rate_limit (ip TEXT NOT NULL, window_minute INTEGER NOT NULL, count INTEGER NOT NULL, PRIMARY KEY (ip, window_minute))`. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/migrations/001_init.sql` — 2 statements, idempotent (`IF NOT EXISTS` on both table + index).
- `/Users/jaychen/cursor/myStar/github/star-app/migrations/002_rate_limit.sql` — 1 statement, composite PK `(ip, window_minute)` matching R10#2 INSERT … ON CONFLICT target.

## Requirements Traced

- **R7#1 (schema)** — `user_state` table defined verbatim per design.md "Data Models → Turso schema": `code TEXT PRIMARY KEY`, `data_json TEXT NOT NULL`, `updated_at INTEGER NOT NULL`, `bytes INTEGER NOT NULL`; plus `idx_user_state_updated_at` index for sweep queries.
- **R7#3 (non-destructive)** — Both files contain only `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`. No `DROP`, no `ALTER … DROP`, no `TRUNCATE`. Safe to re-apply by task 4's idempotent runner.
- **R10#2 (rate_limit table)** — `rate_limit` composite-PK schema in place, matching the `INSERT … ON CONFLICT(ip, window_minute) DO UPDATE SET count = count + 1 RETURNING count` pattern that task 6's `checkAndIncrement` will issue.
