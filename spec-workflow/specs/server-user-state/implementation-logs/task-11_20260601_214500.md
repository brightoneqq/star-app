# Task 11 — Local dev shim: static-file fallback in `scripts/dev.mjs`

**Spec:** server-user-state
**Task ID:** 11
**Completed:** 2026-06-01 21:45:00

## Changes

| File | Action | Notes |
|---|---|---|
| `scripts/dev.mjs` | Modified | Added `node:path` and `fileURLToPath` from `node:url` imports. Added `repoRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..')` resolved once at module init. Added literal `MIME` map: `.html`, `.js`, `.css`, `.json`, `.png`, `.svg`, `.ico`, `.jpg`, `.jpeg`, `.woff2`. Replaced the 501 placeholder branch with a static-file handler: maps `req.url === '/'` to `/index.html`; strips query string (`indexOf('?')` + `slice`); rejects any path segment equal to `..` with 400 `bad path`; resolves via `path.join(repoRoot, urlPath)`; uses `fs.readFile` (async callback) — on `ENOENT` returns 404 `not found`, on other errors returns 500 `internal server error`, on success writes 200 with `Content-Type` looked up from `MIME` (`path.extname(filePath).toLowerCase()`) falling back to `application/octet-stream`, body sent as binary buffer. Top comment updated to reflect that the static fallback is now in place. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/scripts/dev.mjs` — ~120 lines total. Imports `node:http`, `node:fs`, `node:path`, `fileURLToPath` from `node:url`, and `app` from `../server/app.js`. No third-party deps. `/api/*` bridge (task 10) unchanged; all non-API paths now flow through the static handler.
