# Task 18 Implementation Log

**Task:** Surgical edits + script additions in `units/book1/u3/index.html` + `u6/index.html`
**Completed:** 2026-06-01 21:56:55

## Changes

| File | Action | Description |
|------|--------|-------------|
| `units/book1/u3/index.html` | Modified | **Surgical body swap** — Replaced the bodies of the two inline helpers inside the existing top `<script>` block (block opens at line 979). `getStarredCards` body is now a single line: `return MyStar.readJSON('starredCards_u3') || [];`. `saveStarredCards` body is now a single line: `MyStar.writeJSON('starredCards_u3', cards);`. Deleted the inline localStorage try/catch, the `document.cookie.match(...)` cookie-fallback read, and the cookie write (`var d = new Date(); ... document.cookie = 'starredCards_u3=' + encodeURIComponent(val) + ...`) that previously lived inside both function bodies. The rest of the inline `<script>` block — `addTapListener`, `toggleCard`, `toggleAll`, blank-toggle wiring, sort, `globalBtn` handler, post-DCL star restoration — left UNTOUCHED. **Script additions** — After the existing `<script src="../../../js/unit-enhance.js"></script>` line at the bottom and BEFORE `</body>`, inserted three new lines: `<script src="../../../js/sync.js"></script>`, `<script src="../../../js/user-code.js"></script>`, and a tail `<script>` containing a `DOMContentLoaded` handler that calls `MyStar.syncFromServer(function () {})`. No existing `<script>` tag was moved. |
| `units/book1/u6/index.html` | Modified | Same two-part edit applied with `'starredCards_u6'` as the storage slug. `getStarredCards` body collapsed to `return MyStar.readJSON('starredCards_u6') || [];`; `saveStarredCards` body collapsed to `MyStar.writeJSON('starredCards_u6', cards);`. Inline localStorage + cookie-fallback read + cookie write code deleted from both function bodies. The remainder of the top `<script>` block (inline `addTapListener` / `toggleCard` / `toggleAll` / blank-toggle / sort / `globalBtn`) left untouched. Tail script additions identical to u3 (same three lines after the existing `unit-enhance.js` include and before `</body>`). |
| `spec-workflow/specs/server-user-state/tasks.md` | Modified | Marked task 18 as `[X]`. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/units/book1/u3/index.html` — `getStarredCards` (line 980) + `saveStarredCards` (line 984) now route through `MyStar.readJSON` / `MyStar.writeJSON` (so writes flow into the sync engine's debounced `__notifyChange` via task 12's `_touchLocalUpdatedAt`). Tail scripts at lines 1156–1162.
- `/Users/jaychen/cursor/myStar/github/star-app/units/book1/u6/index.html` — same shape with `_u6` slug; tail scripts at lines 1410–1416.
- `/Users/jaychen/cursor/myStar/github/star-app/spec-workflow/specs/server-user-state/tasks.md` — task 18 marked complete.

## Verification

- `grep -n "getStarredCards\|saveStarredCards"` on u3 confirms both functions present at lines 980 and 984; their bodies are now single `MyStar.readJSON`/`MyStar.writeJSON` calls.
- `grep -n "addTapListener\|toggleCard\|toggleAll\|globalBtn"` on u3 confirms all four identifiers still present (lines 988, 1024, 1042, 1134) — block untouched.
- `grep -n "document.cookie"` on u3 returns no matches — all inline cookie code deleted from the two function bodies.
- Same checks for u6: functions at 1249 + 1253; `addTapListener` / `toggleCard` / `toggleAll` / `globalBtn` at 1257 / 1292 / 1308 / 1389; no `document.cookie` references.
- New tail script order on both files: existing `unit-enhance.js` → `sync.js` → `user-code.js` → inline DCL `syncFromServer` — matching the pattern established by task 17 for `index.html` and `review/index.html`.
- No existing `<script>` tag was relocated; the top inline `<script>` block (which holds the DCL handler that restores star state) stays where it was, so its DCL handler fires first and the new tail `syncFromServer` DCL handler fires last.
