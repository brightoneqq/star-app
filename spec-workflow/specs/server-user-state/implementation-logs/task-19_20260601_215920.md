# Task 19 — Steering updates: `product.md`, `tech.md`, `structure.md`

**Spec:** server-user-state
**Task ID:** 19
**Completed:** 2026-06-01 21:59:20

## Changes

| File | Action | Notes |
|---|---|---|
| `spec-workflow/steering/product.md` | Modified | Replaced the entire Non-goals section per design I-1: bare "No accounts, no sync, no backend" is now annotated `(Superseded by server-user-state in 2026-06 — see below.)`; build-pipeline non-goal scoped to **frontend layer**; added new "Optional layers (added 2026-06)" section covering cloud sync via short code and the backend layer. |
| `spec-workflow/steering/tech.md` | Modified | Added new "Backend (optional layer, EdgeOne Pages Functions)" subsection in Stack (Node 24.5.0, Hono ^4.x, @libsql/client ^0.14.x web flavor, ESM in server/ + scripts/ only, two deps total). Appended two new Architectural Decisions: "Server is a sync mirror, not a source of truth" and "Short-code identity is unprotected". Appended two new Known Limitations: Turso outage and short-code threat model. |
| `spec-workflow/steering/structure.md` | Modified | Extended directory layout block with `functions/`, `server/`, `migrations/`, `scripts/`, `package.json`, `.env.example`, `.env` (marked GITIGNORED), plus the new `js/sync.js` and `js/user-code.js` entries. Added three new "Where things go" rows (new backend route, new SQL migration, new piece of server logic). Added two new Anti-patterns (long-lived global state in `server/`; ESM/Node imports in `js/`). |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/spec-workflow/steering/product.md` — Non-goals + Optional layers sections updated; bare "no accounts" line now carries the supersession disclaimer.
- `/Users/jaychen/cursor/myStar/github/star-app/spec-workflow/steering/tech.md` — Backend stack subsection + two architectural decisions + two known limitations appended.
- `/Users/jaychen/cursor/myStar/github/star-app/spec-workflow/steering/structure.md` — Directory layout, "Where things go", and Anti-patterns extended for the backend layer.

## Key Facts

- Prose copied largely verbatim from design.md "Steering Updates (carried by this spec)" section.
- No new hex/color values introduced (out of scope for this task; handled in task 16).
- Frontend constraints (ES5, MyStar.* storage, addTapListener, no fetch) remain UNCHANGED in tech.md — only additive layer documented.
- `.env` listed in structure.md as gitignored to make the boundary explicit to future agents.

## Requirements Traced

- **I-1** — product.md amended so future agents reading steering will NOT rule the feature out of bounds.
- **R7** — backend layer (Node 24.5.0, Hono, @libsql/client, ESM) documented in tech.md Stack section.
- **R8** — frontend constraints (ES5 only in `js/`) reinforced via new Anti-pattern in structure.md.
