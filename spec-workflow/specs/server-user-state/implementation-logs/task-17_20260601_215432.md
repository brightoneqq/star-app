# Task 17 Implementation Log

**Task:** Wire `index.html` + `review/index.html`
**Completed:** 2026-06-01 21:54:32

## Changes

| File | Action | Description |
|------|--------|-------------|
| `index.html` | Modified | **Part A** — Inserted two new `<script>` tags after `<script src="js/shared.js">` (line 35) and BEFORE the existing inline boot `<script>` block: `<script src="js/sync.js"></script>` followed by `<script src="js/user-code.js"></script>`. This ensures `MyStar.setUserCode/getUserCode/syncFromServer/__notifyChange/...` are all attached to `window.MyStar` before the inline `boot()` IIFE runs. **Part B** — Inserted a new tail `<script>` block AFTER the existing inline boot IIFE's closing `</script>` and BEFORE `</body>` containing a `DOMContentLoaded` handler that calls `MyStar.syncFromServer(function () {})`. Because the inline `boot()`'s DCL handler is registered first (when the inline IIFE runs immediately on script load), it fires before the new tail script's DCL handler — this guarantees `M.recordActiveDay()` (now using `writeJSONSilent` via task 12's swap) completes BEFORE `syncFromServer` snapshots the local clock for race-safe comparison. No existing `<script>` tag was moved or altered. |
| `review/index.html` | Modified | **Part A** — Inserted two new `<script>` tags after `<script src="../js/shared.js">` (line 152) and BEFORE the existing inline boot `<script>` block: `<script src="../js/sync.js"></script>` followed by `<script src="../js/user-code.js"></script>`. **Part B** — Inserted a new tail `<script>` block AFTER the existing inline review-page IIFE's closing `</script>` (which contains `loadUnit` + `Promise.all(jobs).then(render)`) and BEFORE `</body>` containing a `DOMContentLoaded` handler that calls `MyStar.syncFromServer(function () {})`. Same DCL ordering guarantee — the existing review-page boot fires first, the new sync trigger fires last. No existing `<script>` tag was moved or altered. |
| `spec-workflow/specs/server-user-state/tasks.md` | Modified | Marked task 17 as `[X]`. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/index.html` — script order now: `manifest.js` → `shared.js` → `sync.js` → `user-code.js` → inline boot IIFE → tail `syncFromServer` DCL script.
- `/Users/jaychen/cursor/myStar/github/star-app/review/index.html` — same script order with `../js/` paths: `manifest.js` → `shared.js` → `sync.js` → `user-code.js` → inline review-page IIFE → tail `syncFromServer` DCL script.
- `/Users/jaychen/cursor/myStar/github/star-app/spec-workflow/specs/server-user-state/tasks.md` — task 17 marked complete.

## Verification

- `index.html` lines 34–38 confirm Part A order: `shared.js` → `sync.js` → `user-code.js` → inline `<script>`.
- `index.html` lines 310–319 confirm Part B placement: existing inline IIFE's `</script>` followed by new `<script>` block with DCL handler, then `</body>`.
- `review/index.html` lines 151–155 confirm Part A order: `shared.js` → `sync.js` → `user-code.js` → inline `<script>`.
- `review/index.html` lines 235–243 confirm Part B placement: existing inline IIFE's `</script>` followed by new `<script>` block with DCL handler, then `</body>`.
- No existing `<script>` tag relocated; existing inline `M.recordActiveDay()` call in `index.html` boot block left untouched (now flows through `writeJSONSilent` via task 12's call-site swap inside `shared.js`).
- DCL registration order: inline boot's DCL handler registers first (synchronous IIFE on script load) → new tail script's DCL handler registers second → DCL event fires both in registration order, so `boot()` runs before `syncFromServer()`.
