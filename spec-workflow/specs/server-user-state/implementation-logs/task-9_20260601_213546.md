# Task 9 — EdgeOne Pages Functions entry shim `functions/api/[[path]].js`

**Spec:** server-user-state
**Task ID:** 9
**Completed:** 2026-06-01 21:35:46

## Changes

| File | Action | Notes |
|---|---|---|
| `functions/api/[[path]].js` | Created | ESM. 3-line shim that imports `app` from `../../server/app.js` and exports `onRequest = (context) => app.fetch(context.request, context.env, context)`. Directory name `[[path]]` is literal (EdgeOne/CF Pages catch-all convention). No business logic — sole purpose is to bridge EdgeOne's Pages Functions signature to Hono's standard 3-arg `fetch(request, env, ctx)` so `server/app.js` remains portable. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/functions/api/[[path]].js` — 4 lines, single re-export. Bridges EdgeOne Pages Functions `onRequest(context)` to `Hono#fetch(request, env, executionCtx)`.

## Verification

- `node --check 'functions/api/[[path]].js'` — clean (no syntax errors).
- Import target `../../server/app.js` exists (verified) and exports `app` as default (per task 7/8).

## Requirements Traced

- **R7 (deploy integration)** — EdgeOne discovers files under `functions/api/` automatically; the `[[path]]` catch-all matches every `/api/*` path so all routes defined in `server/app.js` (health, GET/PUT/DELETE state) reach Hono. If EdgeOne diverges from the documented signature at deploy time, this is the only file that changes — `server/app.js` stays portable.
- **Portability boundary** — Zero business logic in the shim. The web-flavor `@libsql/client` import in `server/db.js` (task 5) is what makes the upstream `app` edge-safe; this shim adds no Node-only APIs.
