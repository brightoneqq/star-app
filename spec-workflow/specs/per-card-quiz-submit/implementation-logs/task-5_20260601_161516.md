# Task 5 Implementation Log

**Task ID**: 5
**Title**: Hook into `setMode` and the unit-level submit handler
**Spec**: per-card-quiz-submit
**Date**: 2026-06-01

## Changes

| File | Change |
|------|--------|
| `js/unit-enhance.js` | Added `injectCardSubmitButtons()` call at the end of the `mode === 'quiz'` branch of `setMode`. |
| `js/unit-enhance.js` | Added `removeCardSubmitButtons()` call to the `else` (review) branch of `setMode`. |
| `js/unit-enhance.js` | Inside the unit-level submit tap handler, inserted per-card score persistence loop (writes `quizScore_<SLUG>_<cid>` for every entry in `result.byCard`) and a single `refreshAllCardBadges(result.byCard)` call, placed between `M.writeNumber(KEY_QTIME, Date.now())` and `showBanner(result)`. |
| `spec-workflow/specs/per-card-quiz-submit/tasks.md` | Marked task 5 as `[X]`. |

## Key Artifacts

- `js/unit-enhance.js` — `setMode(mode)` (quiz branch now wires `injectCardSubmitButtons`; review branch now calls `removeCardSubmitButtons`; unit-level submit handler now persists per-card scores and refreshes badges in one pass via the `byCard` map produced by `gradeScope(document)`).
- `showBanner` and its `banner-retry` handler were left untouched, preserving R6#2 (re-taking the quiz must not clear stored per-card scores or remove header badges).

## Requirements Satisfied

- R1#1, R1#3 — per-card submit lifecycle bound to quiz/review mode transitions.
- R5#1, R5#2, R5#3, R5#4, R5#5 — unit-level submit now also persists per-card scores and refreshes all badges from the single `byCard` walk.
- R6#1 — review-mode cleanup removes per-card submit UI.
- R6#2 — `banner-retry` handler untouched.
- R7#3 — no homepage/review-page edits.
