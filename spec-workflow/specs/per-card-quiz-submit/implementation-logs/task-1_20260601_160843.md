# Task 1 Implementation Log

## Task ID
1 — Add per-card-submit and score-badge styles to `style/unit-enhance.css`

## Files Modified

| File | Change |
|------|--------|
| `style/unit-enhance.css` | Appended three CSS blocks at end of file (after the `@media (max-width: 768px)` rule, line 181) |

## Key Changes

- `.quiz-card-submit-wrap` — flex container, right-aligned, `margin-top: 12px`. Hidden via `body:not(.quiz-mode) .quiz-card-submit-wrap { display: none; }` so per-card submits only appear in quiz mode.
- `.quiz-submit.quiz-submit--card` — compact variant of the existing `.quiz-submit` CTA: `padding: 8px 18px`, `font-size: 0.9rem`, softer `box-shadow: 0 4px 12px rgba(30, 95, 184, 0.18)` (same navy as `--accent-navy` #1E5FB8, permitted as a softer-shadow variant per colors.md rule #6). Inherits navy gradient + base styles from `.quiz-submit`.
- `.card-score-badge` — pill badge (`border-radius: 999px`, `padding: 2px 10px`, `font-size: 0.78rem`, `font-weight: 600`, `margin-right: 8px`) inserted as first child of `.card-actions`.
- Three tier classes using only existing tokens from `style/shared.css :root`:
  - `.tier-high` → `--accent-green-soft` / `--accent-green`
  - `.tier-mid`  → `--accent-amber-soft` / `--accent-amber`
  - `.tier-low`  → `--neutral-gray-soft` / `--text-secondary`

## Conventions Compliance

- No new hex colors — all colors via existing `var(--*)` tokens declared in `style/shared.css`.
- No `border-left` decoration added (colors.md rule #8).
- `rgba(30, 95, 184, 0.18)` is an allowed softer-shadow variant of `--accent-navy`, not a new theme color (colors.md rule #6).
- Plain CSS only — no preprocessor.
- No other files modified.

## Requirements Satisfied

- **R1#5** — Per-card submit button visual layer (`.quiz-card-submit-wrap`, `.quiz-submit--card`) shown only in quiz mode.
- **R4#5** — Per-card last-score badge visual layer (`.card-score-badge` + three tier classes).

## Verification

- Verified all four CSS tokens (`--accent-green`, `--accent-green-soft`, `--accent-amber`, `--accent-amber-soft`, `--neutral-gray-soft`, `--text-secondary`) are declared in `style/shared.css :root`.
- Verified existing `.quiz-submit` rule exists at `style/unit-enhance.css:122` — new `.quiz-submit--card` variant inherits from it without overriding gradient/color.
- Appended cleanly after the existing terminal `@media` block; no edits elsewhere in the file.
