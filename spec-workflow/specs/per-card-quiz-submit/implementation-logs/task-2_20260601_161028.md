# Task 2 Implementation Log

**Task ID:** 2
**Title:** Refactor `gradeQuiz` into `gradeScope(scopeEl)` returning a `byCard` map
**Date:** 2026-06-01

## Changes

| File | Change |
|------|--------|
| `js/unit-enhance.js` | Replaced `gradeQuiz` (old body at lines 104–128) with a new `gradeScope(scopeEl)` function plus a thin `gradeQuiz()` wrapper. Added private ES5 helper `closestVocabCardWithId(el)` that walks `parentNode` (avoids `Element.closest` for old iPad Safari compatibility). `gradeScope` reads inputs via `scopeEl.querySelectorAll('.quiz-input')`, preserves identical `.correct/.wrong` class + `.quiz-hint` mutation logic, and — only when `scopeEl === document` — accumulates a `byCard` map of `{ correct, total, score }` keyed by the nearest ancestor `.vocab-card[id]`. When `scopeEl !== document`, `byCard` is `null`. Return shape is `{ score, correct, total, byCard }`. Existing unit-level submit call site (`var result = gradeQuiz();`) requires no changes — wrapper preserves signature; `byCard` is additive. |
| `spec-workflow/specs/per-card-quiz-submit/tasks.md` | Marked task 2 as `[X]`. |

## Key Artifacts

- `/Users/jaychen/cursor/myStar/github/star-app/js/unit-enhance.js`
  - `closestVocabCardWithId(el)` — new private ES5 helper, walks `parentNode` for nearest `.vocab-card` with non-empty `id`.
  - `gradeScope(scopeEl)` — new function. Returns `{ score, correct, total, byCard }`. `byCard` is a `{ cardId: { correct, total, score } }` map when `scopeEl === document`, otherwise `null`.
  - `gradeQuiz()` — preserved name; now `function gradeQuiz() { return gradeScope(document); }`. Existing call site at the unit-level submit handler unchanged.
- `/Users/jaychen/cursor/myStar/github/star-app/spec-workflow/specs/per-card-quiz-submit/tasks.md` — task 2 marked complete.

## Requirements Satisfied

- **R2#1** — Per-card grading is scope-restricted: `gradeScope(cardEl)` only iterates `cardEl.querySelectorAll('.quiz-input')`, so inputs outside `cardEl` are untouched.
- **R2#2** — Same grading primitive: `MyStar.isAnswerCorrect` is the only matcher used inside `gradeScope`; no new normalization path.
- **R2#3** — Correct inputs get `.correct` class added and prior `.quiz-hint` sibling removed (identical to old `gradeQuiz`).
- **R2#4** — Wrong inputs get `.wrong` class and a fresh `.quiz-hint` sibling `'✗ ' + expected`, replacing any prior hint (identical to old `gradeQuiz`).
- **R7#1** — No existing `quizScore_<slug>` / `quizTime_<slug>` storage values are migrated, rewritten, or removed by this refactor.
- **R7#2** — `quizScore_<slug>` semantics unchanged: the wrapper `gradeQuiz()` returns the same `{ score, correct, total }` numbers the unit-level submit already writes; the additive `byCard` field doesn't alter how the existing handler computes or writes the aggregate.

## Validation

- Verified no ES6 tokens (`const`, `let`, `=>`, template literals, `import`, `export`, `Promise`, spread) anywhere in `js/unit-enhance.js`.
- `gradeScope` return shape matches design §"Data Models → gradeScope extended return".
- `closestVocabCardWithId` avoids `Element.closest` (iPad Safari compatibility).
- `gradeQuiz()` preserved as a thin wrapper; the call site at the unit-level submit handler (`var result = gradeQuiz();`) needs no changes.
