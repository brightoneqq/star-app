{
    "name": "star-app",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "build": "echo 'no build step — EdgeOne KV needs no migration'",
        "dev":   "node scripts/dev.mjs",
        "test":  "echo 'no test runner — verification is manual + Playwright in spec-test phase' && exit 0"
    },
    "dependencies": {
        "hono": "^4.6.0"
    }
}
